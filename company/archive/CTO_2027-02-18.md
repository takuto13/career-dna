# CTOへの指示書 2027-02-18（水）

作成日：2026-08-03（29週後タスク）
担当：CTO
実行予定日：2027-02-18（水）

---

## ステータス：[ ] 未着手

---

## 本日のタスク

### タスク1：disclaimer.html の dateModified を 2027-02-18 に更新

`disclaimer.html` の JSON-LD（WebPage）の `dateModified` を以下に変更：

```json
"dateModified": "2027-02-18"
```

### タスク2：privacy.html の dateModified を 2027-02-18 に更新

`privacy.html` の JSON-LD（WebPage）の `dateModified` を以下に変更：

```json
"dateModified": "2027-02-18"
```

### タスク3：about.html の dateModified を 2027-02-18 に更新

`about.html` の JSON-LD（AboutPage）の `dateModified` を以下に変更：

```json
"dateModified": "2027-02-18"
```

### タスク4：contact.html の dateModified を 2027-02-18 に更新

`contact.html` の JSON-LD（ContactPage）の `dateModified` を以下に変更：

```json
"dateModified": "2027-02-18"
```

### タスク5：sitemap.xml の lastmod を 2027-02-18 に更新

`sitemap.xml` の disclaimer.html / privacy.html / about.html / contact.html の `<lastmod>` を以下に変更：

```xml
<lastmod>2027-02-18</lastmod>
```

### タスク6：llms.txt に 2027-02-18 セクション追加

`llms.txt` の更新履歴に以下を追記：

```
## 2027-02-18
- disclaimer.html / privacy.html / about.html / contact.html の dateModified を 2027-02-18 に更新
- sitemap.xml の各ページ lastmod を 2027-02-18 に更新
```

---

## 注意事項

- 実装後は `git add -A && git commit -m "CTO定例 2027-02-18" && git push origin main` を実行
- BACKLOGの該当タスクを `[x]` に更新してからコミットに含める
