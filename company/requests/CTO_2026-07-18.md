# CTO指示書 2026-07-18

作成日：2026-07-16（CEO定例第2セッション）
実施予定：2026-07-18
担当：CTO

---

## 背景・優先状況

- **FAQ STOP ORDER継続中：** faq.html・タイプページへのFAQ追加は禁止（Issue #9）
- **Issue #7（SEO強化）対応：** jobs.html・全8タイプページのtitle/meta最適化が最優先
- **Issue #16（X自動投稿）対応：** たくとから自動投稿ガイドラインが追加指示あり→スクリプトへの組み込みが必要
- **本日の方針：** SEOタイトル最適化（KW強化）＋ X自動投稿バリデーション機能追加

---

## タスク1：jobs.html title / meta description 最適化（Issue #7対応）

**背景：**
jobs.html（転職エージェント比較ページ）は収益直結の主要ページだが、現在のtitleに「タイプ別」「転職エージェント比較」等の主要KWが不足している可能性がある。
「タイプ別 おすすめ転職エージェント 比較」は競合薄のロングテールKW。

**実装内容：**

jobs.htmlのtitleを以下のフォーマットに変更（現在のtitleを確認して最適化）：

```html
<!-- 変更案（現在のtitleを確認して必要に応じて調整） -->
<title>タイプ別おすすめ転職エージェント比較｜キャリア診断で見つける最適サービス｜キャリアDNA</title>
```

meta descriptionも以下のKWを含む形に最適化：

```html
<meta name="description" content="8タイプ診断で分かった自分のキャリアタイプに合う転職エージェントを比較。戦略家・分析者・職人・調整役・挑戦者・表現者・守護者・育成者タイプ別に最適なサービスを無料で診断。登録不要・5分で完了。">
```

**狙いKW：** 「タイプ別 転職エージェント 比較」「転職エージェント おすすめ タイプ診断」「向いてる転職エージェント 診断」

---

## タスク2：全8タイプページ title 統一最適化（Issue #7対応）

**背景：**
各タイプページのtitleを「転職・向いてる仕事」KWを含む統一フォーマットに整備することで、
「[タイプ名] 向いてる仕事」「[タイプ名] 転職」系ロングテールKWでのGoogle表示強化。

**実装内容：**

全8タイプページのtitleを以下のフォーマットに統一（現在のtitleを確認して必要な場合のみ変更）：

| ページ | タイプ名 | 変更後title（案） |
|--------|---------|----------------|
| type-strategist.html | 戦略家 | `戦略家タイプの向いてる仕事・転職エージェント・強み｜キャリアDNA` |
| type-analyst.html | 分析者 | `分析者タイプの向いてる仕事・転職エージェント・強み｜キャリアDNA` |
| type-craftsman.html | 職人 | `職人タイプの向いてる仕事・転職エージェント・強み｜キャリアDNA` |
| type-mediator.html | 調整役 | `調整役タイプの向いてる仕事・転職エージェント・強み｜キャリアDNA` |
| type-challenger.html | 挑戦者 | `挑戦者タイプの向いてる仕事・転職エージェント・強み｜キャリアDNA` |
| type-creator.html | 表現者 | `表現者タイプの向いてる仕事・転職エージェント・強み｜キャリアDNA` |
| type-guardian.html | 守護者 | `守護者タイプの向いてる仕事・転職エージェント・強み｜キャリアDNA` |
| type-nurturer.html | 育成者 | `育成者タイプの向いてる仕事・転職エージェント・強み｜キャリアDNA` |

**注意：** 現在のtitleを確認し、すでにKWが含まれている場合は不要な変更を避けること。
変更した場合はdateModifiedも更新する。

---

## タスク3：X自動投稿スクリプトへのガイドライン組み込み（Issue #16対応）

**背景：**
たくとから Issue #16 にて自動投稿の注意事項が明示された。
既存の `scripts/post_to_x.py`（コメントアウト状態）にガイドラインとバリデーションロジックを追加し、
たくとのAPI登録完了後に即時利用可能な状態を整える。

**実装内容：**

`scripts/post_to_x.py` を以下の方針で更新：

### 1. ファイル冒頭にガイドラインコメントを追加

```python
# ============================================================
# キャリアDNA X（Twitter）自動投稿スクリプト
# ============================================================
# 【投稿ガイドライン（Issue #16 たくと指示）】
# NG: AIらしい言葉遣い（例：「〜ですよね。」「〜しましょう。」等の機械的な表現）
# NG: 絵文字の多用（目安：1投稿あたり2個以内）
# NG: 炎上が予想される言葉（政治・宗教・差別語等）
# NG: 他社名・他サービス名を出す投稿
# 要確認: 時事ネタを使う場合は事前にたくとへ確認が必要
# 必須: 予約投稿した際は必ずたくとへ報告する
# NG: ハッシュタグのつけすぎ（目安：1投稿あたり2個以内）
# NG: 長文投稿（目安：140文字以内を厳守、最大でも200文字以内）
# ============================================================
```

### 2. バリデーション関数を追加

以下のバリデーション関数をスクリプトに追加（コメントアウトされたまま動作させないが、ロジックは完成させる）：

```python
def validate_post(text):
    """
    投稿前にガイドライン違反がないかチェックする関数
    Returns: (bool, list[str]) - (OKかどうか, 警告メッセージリスト)
    """
    warnings = []
    
    # 文字数チェック（140文字以内が目安）
    if len(text) > 200:
        warnings.append(f"⚠️ 長文: {len(text)}文字（目安200文字以内）")
    elif len(text) > 140:
        warnings.append(f"ℹ️ やや長め: {len(text)}文字（目安140文字以内）")
    
    # ハッシュタグ数チェック
    hashtag_count = text.count('#')
    if hashtag_count > 3:
        warnings.append(f"⚠️ ハッシュタグ多すぎ: {hashtag_count}個（目安2個以内）")
    
    # 絵文字数チェック（簡易版）
    import unicodedata
    emoji_count = sum(1 for c in text if unicodedata.category(c) == 'So')
    if emoji_count > 4:
        warnings.append(f"⚠️ 絵文字多すぎ: {emoji_count}個（目安2個以内）")
    
    # AI的な言い回し警告（代表的なパターン）
    ai_patterns = ['〜しましょう', '〜ですね。', '〜ですよね。', 'ぜひ活用', 'お役立て']
    for pattern in ai_patterns:
        if pattern in text:
            warnings.append(f"⚠️ AI的な表現の可能性: 「{pattern}」")
    
    # 他社名チェック（主要競合を事故防止のためチェック）
    competitor_keywords = ['リクナビ', 'マイナビ転職', 'doda', 'ビズリーチ', 'Indeed', 'エン転職']
    for kw in competitor_keywords:
        if kw in text:
            warnings.append(f"⚠️ 他社名の可能性: 「{kw}」→ 削除を検討してください")
    
    is_ok = len([w for w in warnings if w.startswith('⚠️')]) == 0
    return is_ok, warnings
```

### 3. .github/workflows/auto_post_x.yml の更新

ワークフローファイルに以下のコメントを追加（動作はまだコメントアウト状態を維持）：

```yaml
# 【Issue #16 対応状況】
# 投稿ガイドライン（AIっぽさ禁止・絵文字制限・ハッシュタグ制限・140文字以内）は
# scripts/post_to_x.py の validate_post() 関数に実装済み
# 
# 本番稼働のために必要なステップ（たくとへ依頼）：
# 1. X Developer Portal でアプリ作成（https://developer.twitter.com）
# 2. API Key / Secret / Access Token / Access Token Secret を取得
# 3. GitHub Secrets に以下を設定：
#    - X_API_KEY
#    - X_API_SECRET
#    - X_ACCESS_TOKEN
#    - X_ACCESS_TOKEN_SECRET
# 4. このワークフローのコメントアウトを解除
```

**注意：**
- スクリプトは引き続きコメントアウト状態を維持（実際の投稿はしない）
- バリデーション関数は完成させるが、実行はしない
- 変更後 git commit/push まで実施

---

## タスク4：sitemap.xml lastmod更新 + llms.txt整合確認

**内容：**
- タスク1・2で変更したページの lastmod を `2026-07-18` に更新
- llms.txt の内容（title一覧等）が実際のHTMLと整合しているか確認
- 不整合があれば llms.txt のtitle表記を修正

---

## 確認ポイント（STOP ORDER・禁止事項）

- ⛔ **faq.htmlへのFAQ追加は禁止（Issue #9 STOP ORDER継続中）**
- ⛔ **タイプページへのFAQ追加は禁止（同上）**
- ⛔ **X自動投稿スクリプトの実際の稼働は禁止（API Keys未設定）**
- ✅ title / meta description の変更はOK
- ✅ JSON-LDの更新はOK
- ✅ scripts/post_to_x.py へのバリデーション関数追加はOK
- ✅ sitemap.xml / llms.txt 更新はOK

---

## 期待成果

1. jobs.html title最適化 → 「タイプ別 転職エージェント 比較」KWでのGoogle表示
2. 全8タイプページ title統一 → 「[タイプ名] 向いてる仕事」「[タイプ名] 転職」ロングテールKWでの表示強化
3. X自動投稿スクリプトにバリデーション機能追加 → たくとのAPI申請完了後に即座に安全な自動投稿が可能な状態
4. sitemap.xml更新 → Google Search Consoleでの再クロール促進

---

## 完了後の処理

1. BACKLOGの該当タスクを `[x]` に更新
2. 本指示書を `company/archive/` に移動
3. git add / commit / push まで実施
